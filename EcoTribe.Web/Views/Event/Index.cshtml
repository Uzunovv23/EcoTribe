@model List<EcoTribe.BusinessObjects.ViewModels.EventViewModel>

@{
    ViewData["Title"] = "Events";
}

<h2 class="mb-4">Events</h2>

@if (Model != null && Model.Any())
{
    <table class="table table-striped">
        <thead>
            <tr>
                <th>#</th>
                <th>Name</th>
                <th>City</th>
                <th>Type</th>
                <th>Volunteers Required</th>
                <th>Start</th>
                <th>End</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var eventItem in Model)
            {
                <tr>
                    <td>@eventItem.Id</td>
                    <td>@eventItem.Name</td>
                    <td>@eventItem.City</td>
                    <td>@eventItem.Type</td>
                    <td>@eventItem.RequiredVolunteers</td>
                    <td>@eventItem.Start.ToString("yyyy-MM-dd HH:mm")</td>
                    <td>@eventItem.End.ToString("yyyy-MM-dd HH:mm")</td>
                    <td>
                        <a href="@Url.Action("Details", "Event", new { id = eventItem.Id })" class="btn btn-info btn-sm">Details</a>
                        <a href="@Url.Action("Edit", "Event", new { id = eventItem.Id })" class="btn btn-warning btn-sm">Edit</a>
                        <a href="@Url.Action("Delete", "Event", new { id = eventItem.Id })" class="btn btn-danger btn-sm" onclick="return confirm('Are you sure?');">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>No events available.</p>
}

<a href="@Url.Action("Create", "Event")" class="btn btn-primary mt-3">Create New Event</a>
